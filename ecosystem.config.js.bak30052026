module.exports = {
  apps: [
    // 1. VITRINE (Next.js)
    {
      name: 'nextltd-vitrine',
      cwd: '/var/www/numericexport/vitrine',
      script: 'npm',
      args: 'start',
      instances: 1,
      exec_mode: 'fork',
      autorestart: true,
      max_memory_restart: '1G',
      env: {
        NODE_ENV: 'production',
        PORT: 3000,
      }
    },

    // 2. API PRINCIPALE - 2 instances en cluster
    {
      name: 'nextltd-api',
      cwd: '/var/www/numericexport/api',
      script: 'src/index.js',
      instances: 2,
      exec_mode: 'cluster',
      watch: false,
      max_memory_restart: '1G',
      env: {
        NODE_ENV: 'production',
        PORT: 3001,
      }
    },

    // 3. ✅ WORKER WHATSAPP UNIFIÉ (message.worker.js)
    {
      name: 'whatsapp-worker',
      cwd: '/var/www/numericexport/api',
      script: 'src/workers/start.js',  // ✅ Point d'entrée unique
      instances: 1,
      exec_mode: 'fork',
      autorestart: true,
      max_memory_restart: '1G',
      env: {
        NODE_ENV: 'production',
      }
    },

    // 4. CRONS (Storage Cleaner)
    {
      name: 'storage-cleaner',
      cwd: '/var/www/numericexport/api',
      script: 'src/cron/storage-cleaner.js',
      instances: 1,
      exec_mode: 'fork',
      autorestart: false,
      cron_restart: '0 0 * * *', // Tous les jours à minuit
      env: {
        NODE_ENV: 'production',
      }
    },

  {
  name: 'cron-clean-messages',
  cwd: '/var/www/numericexport/api',
  script: 'src/cron/clean-old-messages.js',
  instances: 1,
  exec_mode: 'fork',
  autorestart: false,           // ne redémarre pas en boucle
  cron_restart: '0 4 * * *',    // tous les jours à 4:00
  env: {
    NODE_ENV: 'production'
  }
},

    // 5. DASHBOARD (Next.js)
    {
      name: 'nextltd-dashboard',
      cwd: '/var/www/numericexport/dashboard',
      script: 'npm',
      args: 'start -- -p 3003',
      instances: 1,
      exec_mode: 'fork',
      autorestart: true,
      max_memory_restart: '1G',
      env: {
        NODE_ENV: 'production',
        PORT: 3003,
      }
    }
  ]
};

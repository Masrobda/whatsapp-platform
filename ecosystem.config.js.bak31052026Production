module.exports = {
  apps: [
    // 1. VITRINE (Next.js)
    {
      name: 'nextltd-vitrine',
      cwd: '/var/www/numericexport/vitrine',
      script: 'npm',
      args: 'start',
      instances: 1,
      exec_mode: 'fork',
      autorestart: true,
      max_memory_restart: '1G',
      env: {
        NODE_ENV: 'production',
        PORT: 3000,
      }
    },

    // 2. API PRINCIPALE - 2 instances en cluster
    {
      name: 'nextltd-api',
      cwd: '/var/www/numericexport/api',
      script: 'src/index.js',
      instances: 2,
      exec_mode: 'cluster',
      watch: false,
      max_memory_restart: '1G',
      env: {
        NODE_ENV: 'production',
        PORT: 3001,
      }
    },

    // 3. WORKER WHATSAPP UNIFIÉ
    {
      name: 'whatsapp-worker',
      cwd: '/var/www/numericexport/api',
      script: 'src/workers/start.js',
      instances: 1,
      exec_mode: 'fork',
      autorestart: true,
      max_memory_restart: '1G',
      env: {
        NODE_ENV: 'production',
      }
    },

    // 4. AUTOMATION CRON (Ajouté)
    {
      name: 'automation-cron',
      cwd: '/var/www/numericexport/api',
      script: 'src/workers/automation.cron.js',
      instances: 1,
      exec_mode: 'fork',
      autorestart: true,
      max_memory_restart: '500M',
      env: {
        NODE_ENV: 'production',
      }
    },

    // 5. CAMPAIGN CRON (Ajouté)
    {
      name: 'campaign-cron',
      cwd: '/var/www/numericexport/api',
      script: 'src/workers/campaign.cron.js',
      instances: 1,
      exec_mode: 'fork',
      autorestart: true,
      max_memory_restart: '500M',
      env: {
        NODE_ENV: 'production',
      }
    },

    // 6. STATS WORKER (Ajouté)
    {
      name: 'stats-worker',
      cwd: '/var/www/numericexport/api',
      script: 'worker-stats.js',
      instances: 1,
      exec_mode: 'fork',
      autorestart: true,
      max_memory_restart: '500M',
      env: {
        NODE_ENV: 'production',
      }
    },

    // 7. INBOX WORKER (Ajouté - Ajuste le script si find a donné un autre dossier)
    {
      name: 'inbox-worker',
      cwd: '/var/www/numericexport/api',
      script: 'src/workers/inbox-sync-worker.js', 
      instances: 1,
      exec_mode: 'fork',
      autorestart: true,
      max_memory_restart: '500M',
      env: {
        NODE_ENV: 'production',
      }
    },

    // 8. CRONS DE NETTOYAGE (Lancés à intervalles fixes)
    {
      name: 'storage-cleaner',
      cwd: '/var/www/numericexport/api',
      script: 'src/cron/storage-cleaner.js',
      instances: 1,
      exec_mode: 'fork',
      autorestart: false,
      cron_restart: '0 0 * * *',
      env: {
        NODE_ENV: 'production',
      }
    },
    {
      name: 'cron-clean-messages',
      cwd: '/var/www/numericexport/api',
      script: 'src/cron/clean-old-messages.js',
      instances: 1,
      exec_mode: 'fork',
      autorestart: false,
      cron_restart: '0 4 * * *',
      env: {
        NODE_ENV: 'production'
      }
    },

    // 9. DASHBOARD (Next.js)
    {
      name: 'nextltd-dashboard',
      cwd: '/var/www/numericexport/dashboard',
      script: 'npm',
      args: 'start -- -p 3003',
      instances: 1,
      exec_mode: 'fork',
      autorestart: true,
      max_memory_restart: '1G',
      env: {
        NODE_ENV: 'production',
        PORT: 3003,
      }
    }
  ]
};
